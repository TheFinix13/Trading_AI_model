---
description: Guard against concurrent Cursor sessions clobbering shared state files (ai_context.md, config registries) in this repo
alwaysApply: true
---

# Concurrent-session safety

Canonical protocol: `/Users/the1finix/Documents/GitHub/brain-box/agents/concurrent-session-safety.md`.

This repo, and its research sibling `finance-research-experiments`, can
have more than one Cursor session open at once — this actually happened
on 2026-07-01, when a research-pipeline-sweep session raced an unrelated
M001 session on shared files in `finance-research-experiments`. Git does
not protect you here: no branch divergence, just a race on one working
tree, and `git commit` can silently discard the other session's edits.

Before editing or committing `ai_context.md`, `docs/CHECKPOINT.md`, or
any other file you didn't create earlier in this same session:

1. Re-read it fresh immediately before editing.
2. Prefer additive edits over full rewrites.
3. Never `git add -A` / `git add .` — stage explicit paths only.
4. Before committing, run `git diff --cached <file>` and confirm every
   line is yours; unrecognized content means another session is active
   — stop and reconcile by hand, don't overwrite.
5. Commit in small units rather than one end-of-session batch.

## Session claim ritual (added 2026-07-14)

The canonical protocol now includes a session-claim mechanism — full
detail in `agents/concurrent-session-safety.md` §1–§2 (brain-box).

At session start, in EVERY repo this session will write to (this repo,
and siblings like `finance-research-experiments` or brain-box if you'll
log there):

1. `git status --short` + `git log --oneline -5` — unrecognized
   uncommitted work or same-day unrelated commits = another session;
   treat foreign paths as untouchable (never clean/stash/commit/revert
   them).
2. Read `.sessions/` claim files; overlapping scope → narrower claim
   wins the contested paths; genuine same-file conflict → ask the user.
3. Drop your own claim: `.sessions/<date>_<topic>.md` (started-at,
   anchor, topic, write-scope, status). Mark done or delete at session
   end. `.sessions/` is gitignored (README inside has the format).

Never commit another session's uncommitted work even if it looks
finished. If a file changes under you mid-session, stop, snapshot your
version, three-way diff, and hand both-touched hunks to the user — never
guess-merge.

See also: `/Users/the1finix/Documents/GitHub/brain-box/.cursor/rules/concurrent-session-safety.mdc`
