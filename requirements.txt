# Live-deployment requirements (Windows VM / demo account).
# Mirrors the runtime dependencies in pyproject.toml plus the Windows-only
# MT5 connector. For development installs prefer: pip install -e .
pandas>=2.2,<3
numpy>=1.26
pyarrow>=15.0
pydantic>=2.6
python-dotenv>=1.0
PyYAML>=6.0
scikit-learn>=1.4
joblib>=1.3
xgboost>=2.0
matplotlib>=3.8
# mplfinance only publishes pre-release versions (0.12.xbY), so the floor
# must name one explicitly or pip refuses to match it.
mplfinance>=0.12.10b0
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.20
rich>=13.7
click>=8.1
schedule>=1.2
yfinance>=0.2.40
dukascopy-python>=4.0
httpx>=0.27
shap>=0.45
MetaTrader5>=5.0.45 ; sys_platform == 'win32'
# Sprint 1 F006 (per D052) -- OS-keychain-first credential storage.
# `keyring` uses the OS native store (macOS Keychain / Windows Credential
# Manager / Freedesktop Secret Service) with a Python fallback. Zero-cost,
# BSD-3, mature (10+ years). No new SaaS relationship.
keyring>=25.0
# F006's encrypted-file fallback (Fernet) imports this directly in
# agent/platform/credentials.py -- it was missing here until the
# 2026-07-24 VM cutover hit ModuleNotFoundError on a clean venv.
cryptography>=42.0

