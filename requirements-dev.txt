# Development-only extras — NOT installed on the VM.
# The platform E2E browser test (tests/test_platform_e2e.py) needs:
#   pip install -r requirements-dev.txt
#   python -m playwright install chromium
# The test self-skips when playwright/chromium is unavailable, so CI/VM
# runs without these stay green.
pytest>=8.0
playwright>=1.40
